server:
  port: 8888

logging:
  path: logs
  file: userManager.log

spring:
  application:
    name: userManager #程序的名字
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/xdclass?useUnicode=true&characterEncoding=utf8
    username: root
    password: 123

mybatis:
  type-aliases-package: com.course.model
  mapper-locations:
    - mapper/* #映射mapper文件夹
